# --- Deep learning / vision ---
numpy==1.26.4
opencv-python==4.11.0.86
matplotlib==3.10.9

# PyTorch CUDA 12.8 (à installer avec --extra-index-url https://download.pytorch.org/whl/cu128)
# cu128 est le premier build embarquant les kernels sm_120 (GPU Blackwell, ex. RTX PRO 3000).
# Les roues cu118 ne vont que jusqu'à sm_90 -> "no kernel image is available for execution on the device".
torch==2.9.1+cu128
torchvision==0.24.1+cu128

ultralytics==8.4.56
ultralytics-thop==2.0.20

# Pas de pyorbbecsdk : la caméra tourne dans un conteneur wrapper séparé
# qui publie les images sur des topics ROS2 (sensor_msgs/Image via cv_bridge)

# --- Optionnel : outils de test/lint ROS2 (colcon test), pas nécessaires à l'exécution ---
# pytest
# ament_copyright
# ament_flake8
# ament_pep257